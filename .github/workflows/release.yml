name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: ""
          - platform: macos-latest
            args: "--target aarch64-apple-darwin"
          - platform: macos-latest
            args: "--target x86_64-apple-darwin"

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install frontend dependencies
        run: npm ci

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "KeyOverlay ${{ github.ref_name }}"
          releaseBody: |
            ## Novedades
            Consulta el [README](https://github.com/${{ github.repository }}/blob/main/README.md) para la documentación completa.

            ### Windows
            Descarga **KeyOverlay_x.x.x_x64-setup.exe** en la sección **Assets** de abajo.
            > ⚠️ Windows puede mostrar una advertencia de SmartScreen — haz clic en **"Más información → Ejecutar de todas formas"**. Esto es normal en apps de código abierto sin firmar.

            ### macOS
            Descarga el **.dmg** según tu chip:
            - **Apple Silicon (M1/M2/M3):** `_aarch64.dmg`
            - **Intel:** `_x64.dmg`
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
